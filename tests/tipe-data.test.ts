describe('Data type', () => {
    it('should must declare', () => {
        const name: string = 'Ahmad Lutfi Rizki Patria';
        const balance: number = 1000000;
        const isVip: boolean = true;

        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
