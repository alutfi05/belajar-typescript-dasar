describe('Array', () => {
    it('should same with javascript', () => {
        const names: string[] = ['Ahmad', 'Lutfi', 'Laila'];
        const values: number[] = [1, 2, 3];

        console.info(names);
        console.info(values);
    });
});
